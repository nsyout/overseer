{
  "name": "overseer-mcp",
  "version": "0.1.0",
  "description": "MCP server for Overseer task orchestration",
  "type": "module",
  "bin": {
    "overseer-mcp": "./build/index.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "prepare": "npm run build",
    "test": "node --test build/tests/**/*.test.js"
  },
  "keywords": ["mcp", "task-management", "agent"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=18"
  }
}
